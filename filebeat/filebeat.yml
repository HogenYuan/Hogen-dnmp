filebeat.inputs:
    - type: log
      paths:
        - /www/wwwlogs/*.log
      multiline.pattern: '^\['
      multiline.negate: true
      multiline.match: after
      # fields:
      #     index: 'laravel-log'
    # setup.template.settings:
      # index.number_of_shards: 1
    # setup.dashboards.enabled: false
    # setup.kibana:
      # host: "http://34.96.172.25:5601"
    # 不直接传输至ES
    #output.elasticsearch:
    # hosts: ["http://es-master:9200"]
    # index: "filebeat-%{[beat.version]}-%{+yyyy.MM.dd}"
    # output.logstash:
      # hosts: ["http://34.96.172.25:4560"]
    # output.redis:
    #     hosts: ["127.0.0.1:6379"]
    #     enabled: true
    #     db: "1"
    #     timeout: 5
    #     # password: ""
    #     key: "%{[fields.index]:otherIndex}"
    processors:
      - add_host_metadata: ~
      - add_cloud_metadata: ~
